// 这个文件会被Expo生成的文件覆盖，但是确保目录结构正确
// 这是一个占位符build.gradle文件
apply plugin: "com.android.application"

android {
    compileSdkVersion 33
    defaultConfig {
        applicationId "com.sncc.knowledgeqa"
        minSdkVersion 21
        targetSdkVersion 33
        versionCode 1
        versionName "1.0.0"
    }
}

dependencies {
    implementation "com.facebook.react:react-native:+"
} 